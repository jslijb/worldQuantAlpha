# 挖矿战报与打磨清单（2026-09-08）

> 三轮批量模拟 33 个候选（并发3，testPeriod=P1Y 全部带测试集验证），26 个出结果。
> 质量门槛：全 IS S+F≥4.0 + Test段 Sharpe≥1.25 且不崩 + checks全过 + selfCorr<0.7。
> **结论：0 提交（遵守"低的先别提交"），1 个 A 档候选进打磨队列。**

## 一、最佳候选（差一步）

**78Z1qvnv**（e04）: `group_rank(liabilities_curr/assets, subindustry) + rank(-ts_delta(close, 2))`
- 全 IS：S=2.50 F=1.98 **S+F=4.48 ✅A档** T=0.168
- Train(前4年) S=2.62 / **Test(最后1年) S=1.94 ✅**（≥1.25 且 ≥0.6×train，未崩）
- 其余 checks 全 PASS
- **卡点：相关性预检 max=0.7612 ≥ 0.7** → 与已提交 kqjpepz8(S=2.74)/e79kPeEM(3.02) 家族同源，按文档规则需 Sharpe≥3.01 才能豁免，2.50 不够 → 正确跳过，未浪费提交配额

## 二、全部结果（26 个，按 S+F 降序摘录）

| 候选 | 思路来源 | S | F | S+F | T | testS | 判定 |
|---|---|---|---|---|---|---|---|
| 78Z1qvnv | 负债锚+2日反转 | 2.50 | 1.98 | **4.48** | 0.168 | 1.94 | A档但相关0.76 |
| Vk6PKe6J | 反转×相对放量(group_rank) | 1.95 | 0.95 | 2.90 | 0.600 | 3.21 | 低T方向对，S不够 |
| 6Xr9gemK | c01+decay3 | 1.95 | 0.81 | 2.76 | 0.744 | 3.29 | T爆表 |
| 9qVrgQ52 | 反转+放量条件 | 1.68 | 0.85 | 2.53 | 0.323 | 2.28 | — |
| vRklnrKz | 价值+反转+残差三信号 | 1.43 | 0.93 | 2.36 | 0.286 | 2.28 | — |
| e7908GPN | d10平滑版 | 1.58 | 0.80 | 2.38 | 0.433 | 1.67 | 平滑降T但S同降 |
| （其余20个） | IQC示例5/7/8/9/13等 | <1.9 | <1.0 | <2.6 | — | — | 低于打磨线 |

失败 7 个（权限/字段，文档早有预言"没有权限就找类似替代品"）：
- 算子无权限：`sum`、`ts_stddev` → 已用 `ts_mean`/`ts_rank` 替代修复
- 字段不存在：`net_income`、`cfo`、`etz_eps`、`snt_bullish` → **TUTORIAL 账号确认无分析师/情绪数据集**；真实字段名以已提交 alpha 的 raw JSON 为准（`cashflow_op`/`enterprise_value`/`liabilities_curr`/`fnd6_*`）

## 三、打磨方向（文档级，下一轮执行）

### 78Z1qvnv 降相关（目标：corr<0.7 或 S≥3.02，二选一）
按"降相关 3 招"（改进指南 §五）：
1. **换锚字段**：`liabilities_curr/assets` → `fnd6_mfma2_revt/assets`（你的 883OXpJl 用过，S=2.48 证明可用）或 `assets_curr/assets` 趋势项
2. **换反转算子**：`-ts_delta(close,2)` → `-ts_corr(close,volume,20)` 残差类 或 `ts_regression(close,vwap,60)` 残差（MP1QAz26 已证明残差与锚可叠加）
3. **换分组**：subindustry → industry/sector 变体
- 组合拳示例（下轮 c 候选）：`group_rank(fnd6_mfma2_revt/assets, subindustry) + rank(ts_delay(close,2) - ts_regression(close,vwap,60,rettype=3))`

### Vk6PKe6J 提 Sharpe（T=0.6 是对的，S 差 0.5）
- 它已是"反转×放量"group_rank 形态（T 0.6 但 F 0.95 最高性价比）
- 加第三个低相关信号：`+ group_rank(ts_av_diff(cashflow_op/enterprise_value, 60), subindustry)`（价值趋势项，与短反转低相关）

## 四、明日起每日挖矿 SOP（已跑通全链路）

```
1. 母本池 = 13 IQC示例 + 78Z1qvnv/Vk6PKe6J 打磨版 + 你31个里的A档
2. 变异规则 = 换字段(真实名!)/换算子/换分组/换数据集
3. mine_batch.py 批量模拟（testPeriod=P1Y，并发3）
4. mine_submit.py 自动筛选：S+F≥4.0 + testS≥1.25不崩 + checks全过
   → /correlations/self 免费预检 max<0.7 → submit → 验证 ACTIVE
5. 不过线的进本清单继续打磨，绝不硬交
```

**关键基础设施经验**：
- `testPeriod` 参数格式 = ISO 8601 时长字符串 `"P1Y"`（整数会 400）
- 未提交 alpha 的 checks 里 SELF_CORRELATION 恒非 PASS，属正常，提交时才判
- 提交前必跑 `/correlations/self`（免费），max≥0.7 且 Sharpe 不够 1.1× 时会 403 浪费配额
