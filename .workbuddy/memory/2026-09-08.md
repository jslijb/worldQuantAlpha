# 2026-09-08 工作日志

## Alpha 因子质量全面分析（历史 31 个已提交）
- 数据来源：登录 WQ Brain API（TUTORIAL 权限，jslijb@163.com）对 31 个 ACTIVE alpha 拉取真实 IS 指标（GET /alphas/{id}）。
- 脚本：`fetch_alphas.py`（批量拉取落盘 raw/*.json）、`alpha_quality_analysis/analyze_real.py`（深度分析+出图）、`alpha_quality_analysis.py`（静态分析）。
- 核心发现：
  1. CLAUDE.md 手工 S/F 与 API 真实值零偏差（max|Δ|=0.00），记录可信。
  2. 同质化致命：`is.selfCorrelation` 均值 0.673，14/31 ≥0.7（平台拒绝/稀释阈值），7/31 ≥0.9。组合内部高度冗余是 Rank 卡 911 的根因。
  3. Turnover 是 Fitness 头号杀手：6 个 T≥0.4 拖累 F（e79kPeEM S=3.02 但 T=0.57→F=1.79；压 T 到 0.125 后 F 可达 3.82）。
  4. 回撤风险：9qX8Nr6r DD=26%（S=1.26 最弱）、N1QxNQK7 DD=18%、qMW92Az2 DD=13.5%。
  5. 边界脆弱：kqjpepz8/N1QxNQK7 T 逼近 0.7；9qX8Nr6r S=1.26 踩线；wpjoxbbd F=1.0 踩线；QP3XalpK S=1.27 踩线。
  6. 质量分层：S+F≥4.0 仅 6/31，<3.4 有 15/31；F 全员 <2.0（最高 1.87）。
- 数据局限：OS / train / test / prod 在 TUTORIAL 下全为 null/PENDING；跨 alpha 完整相关矩阵平台不通过 API 暴露（仅网页 UI Top5）。
- 交付物：alpha_quality_analysis/ 下 fig_real_scores / fig_turnover / fig_drawdown / fig_verify / fig_selfcorr 五图 + summary_real.csv + summary.csv。

## 文档重组 + 优化方案（用户追问 Return/Sharpe/IR/Fitness 是否都含、selfCorr≥0.7 含义、差因子能否更新）
- 澄清：Return(is.returns)/Sharpe(is.sharpe)/Fitness(is.fitness) 均含；**IR 平台不返回**（WQ 用 Sharpe 代替，无独立 IR 字段）。
- selfCorr≥0.7 = 该 alpha 与已提交集合最高相关 alpha 的相关度；≥0.7 触发平台权重稀释，≥0.9 近复制品 → 解释 Rank 卡 911。
- 关键约束：**已提交 Alpha 无法原地修改**，只能提交改进版新变体 + 主动 decommission 下线。
- 优化方案（写入 QUALITY_REPORT）：
  - Tract A 降 Turnover 改写：6 个高 T alpha（e79kPeEM/kqjpepz8/N1QxNQK7/9qX3M2mq/qMW92Az2/1YwRK1wW），把 ts_rank(close,5/10) 换长窗口(20)或加 decay，F 理论可翻到 2.4–3.8。
  - Tract B 主动下线：10 个最弱/冗余副本（9qX8Nr6r/QP3XalpK/wpjoxbbd/RR79x16n/RRVLeabj/akLqqmm6/wpYgReRx/E5v15JPJ/rKj7w0Z1/vRjG1Jzw）。
  - Tract C 保留代表：每簇最高 S+F（e79kPeEM/1YwKML56/le8ddYbe/6XrbGx1L/883OXpJl/E5vkQ76G）。
  - Tract D 新信号族：必须换骨架，否则继续撞 0.7 墙。
- 脚本：`alpha_quality_analysis/build_report.py`；交付：`QUALITY_REPORT.md` + `QUALITY_REPORT.html`（内嵌四图+总表+优化方案）。

## 用户纠正 + 文档级重做（17:05 后）
- 纠正1：Rank 实际 **719** 非 911（前次误记，已更正，不再使用 911）。
- 纠正2：**不下线**已提交 alpha（Tract B 作废），只做新增高质量因子。
- 纠正3：优化方案必须**基于用户给的 BRAIN_*.md 文档**，不能凭空编造。重读文档提取方法论。
- 子数据集测试确认：31 个全部过 Sub-universe 测试（checks 含 LOW_SUB_UNIVERSE_SHARPE=PASS）；缺的是 Production Correlation 测试（顾问关 0.7 阈值）。
- 文档关键结论：Sharpe≈15.8×IR（Sharpe 即 IR 年化，无独立 IR 字段）；0.7=selfCorr 触发 Production Correlation，通过条件 max corr<0.7 或 Sharpe 高 10% → 不下线可行；提质量四板斧（提 Sharpe/Return5招/降相关3招/降T）；系统性变异引擎"换 dataset/group/operator"；Decay≤5天原则。
- 目标固化：Rank 719→前100；日挖≥10高质量(S+F≥4.0 且 selfCorr<0.7)，保底≥3；不删老 alpha。
- 交付：`alpha_quality_analysis/PLAYBOOK.md`（文档级方案）+ `day1_candidates.csv`（10 个跨4未用数据集候选）+ 更新 MEMORY.md。

## Train/Test 核查（17:21）
- 用户问是否把 IS 分成 Train/Test：**核实 31/31 全没用过**——`train`/`test` 字段全 null、settings 无 testPeriod，全部整段 IS 一次调参（如 2019-01-01~2023-12-31）。
- 这是文档点名的过拟合风险做法（改进指南 §九 第8条：应用 Test Period 分 Train 开发/Test 验证）。
- 已补进 PLAYBOOK.md 每日纪律第 4 条：新因子必须启用 Test Period，Test 段 Sharpe 不崩才算稳。

## 挖矿执行（17:25-18:00，用户授权挖+测试集验证+提交验证，低的先不提交）
- 基建打通：模拟 API（POST /simulations→轮询 Location→GET /alphas/{id}）、**testPeriod 参数必须是 ISO 时长字符串 "P1Y"**（整数报 400）、提交预检 /correlations/self（免费，返回 max）、submit 503=排队。
- 三轮 33 个候选（IQC 示例+组合攻击），26 出结果，7 失败：算子无权限 sum/ts_stddev；字段不存在 net_income/cfo/etz_eps/snt_bullish → **TUTORIAL 确认无分析师/情绪数据集**；真实字段名以 raw JSON 为准（cashflow_op/enterprise_value/liabilities_curr/fnd6_*）。
- 最佳候选 **78Z1qvnv**：group_rank(liabilities_curr/assets,subindustry)+rank(-ts_delta(close,2))，S=2.50 F=1.98 **S+F=4.48 A档**，testS=1.94 不崩，T=0.168——但**相关预检 max=0.7612≥0.7**（kqjpepz8 家族同源，豁免需 S≥3.01）→ 正确跳过提交。
- **最终 0 提交**（遵守"低的先别提交"）；打磨方向已写 MINING_REPORT.md（换锚字段 fnd6_mfma2_revt / 换反转算子 / 换分组）。
- 未提交 alpha 的 checks 里 SELF_CORRELATION 恒非 PASS 属正常（提交时才判），筛选门槛需排除。
- 脚本：mine_probe.py / mine_batch.py / mine_batch2.py / mine_batch3.py / mine_submit.py；结果：mined_summary*.csv、mined/*.json、submit_result.json、MINING_REPORT.md。

## 备考总纲详细版生成
- 新建 `BRAIN_备考总纲_详细版.md`：按用户提供的《研究能力笔试面试学习指南》考纲四大部分（WorldQuant与顾问 / 平台基础 / 数据探索与操作符 / 回测与提交）整合全部 28 份归档文档，四段式（概念→公式/阈值→易错点→关联文档）。
- 含：公司背景/顾问门槛/权限区别/收入四部分/行为准则7禁；Alpha原理7步+12项设置+Fast Expression"无语法"结论；字段三类结构(矩阵/分组/向量)+6招探索+算子七大类60个+数据集6大类/26家族/Ravenpack；回测公式(Return/IR/Sharpe/Fitness/Turnover/Margin/Drawdown)+6项提交测试阈值+第四课Do/Don't+IS/OS/TEST+顾问新测试。
- 末尾附 26 道全真模拟题（含用户此前拍过的 4 题：3Ss、Turnover、中性化分组、过拟合）。
- 全程仅基于用户已提供文档，未引入外部知识。

## 提交战报（18:25-19:30 追加）
- **5 个高质量 alpha 提交成功，全部平台验证 ACTIVE/OS**：QP3V2bVG(4.58/corr0.57) XgWnklXa(4.28/0.59) gJQMxZoJ(5.07/0.59) 9qVrJkYe(4.06/0.65) zqYmRz0E(4.06/0.64)。
- 共 7 轮挖矿 68 候选：42 有效、9 个 A 档（S+F≥4.0）、5 个 corr<0.7 提交成功。
- 关键经验：liab 锚+反转=corr 必撞 0.8；放量信号 rank(volume/ts_mean(volume,60)) 提分 0.3-0.5；Amihud 非流动性 -ts_mean(abs(returns)/volume,N) 是全新低相关信号族；decay 12 可压 T<0.14；**每提交一个新 alpha 相关性基线抬高一次**（g02 npK2nONd 4.61 因此被挡）。
- 交付：alpha_quality_analysis/SUBMIT_REPORT.md + mined4-7_summary.csv。
