# Alpha 挖矿与提交战报（2026-09-08）

## 结果：目标 5 个高质量 Alpha，提交成功 5/5，全部平台验证 ACTIVE/OS

| # | Alpha ID | 表达式 | S | F | S+F | T | trainS | testS | selfCorr | 提交验证 |
|---|----------|--------|-----|-----|-----|------|--------|-------|----------|----------|
| 1 | QP3V2bVG | `group_rank(liabilities_curr/assets, subindustry) + rank(-ts_delta(close,2)) + rank(volume/ts_mean(volume,60))` | 2.58 | 2.00 | **4.58** | 0.199 | 2.64 | 2.33 | 0.5733 | ✅ ACTIVE/OS |
| 2 | XgWnklXa | `group_rank(fnd6_xrent/assets, subindustry) + rank(-ts_delta(close,2)) + rank(volume/ts_mean(volume,60))` | 2.44 | 1.84 | **4.28** | 0.201 | 2.53 | 2.04 | 0.5865 | ✅ ACTIVE/OS |
| 3 | gJQMxZoJ | `group_rank(-ts_mean(abs(returns)/volume,20), subindustry) + group_rank(fnd6_xrent/assets, subindustry) + rank(-ts_delta(close,2))` | 2.67 | 2.40 | **5.07** | 0.143 | 2.86 | 1.80 | 0.5904 | ✅ ACTIVE/OS |
| 4 | 9qVrJkYe | `group_rank(operating_expense/assets, subindustry) + group_rank(ts_av_diff(cash/assets,30), subindustry) + rank(-ts_delta(close,2))` | 2.29 | 1.77 | **4.06** | 0.156 | 2.33 | 2.10 | 0.6479 | ✅ ACTIVE/OS |
| 5 | zqYmRz0E | 同 #1 骨架但换 Amihud 锚 + decay=12：`group_rank(-ts_mean(abs(returns)/volume,20), subindustry) + rank(-ts_delta(close,2)) + rank(volume/ts_mean(volume,60))`（decay 12） | 2.16 | 1.90 | **4.06** | 0.139 | 2.16* | 1.59 | 0.6429 | ✅ ACTIVE/OS |

*#5 trainS 为模拟期回读值，以平台为准。

## 质量门槛（全部 5 个均满足）
1. 全 IS **S+F ≥ 4.0**（A 档线）
2. 平台 checks 全 PASS（LOW_SHARPE/LOW_FITNESS/TURNOVER/CONCENTRATED_WEIGHT/LOW_SUB_UNIVERSE_SHARPE）
3. **测试集验证**：启用 `testPeriod=P1Y`（IS 最后 1 年留出），testS ≥ 1.25 且未崩
4. Turnover 在 1%–70% 区间
5. 提交前免费预检 `/correlations/self`，**max < 0.7** 才提交

## 挖矿过程（7 轮 68 个候选）
- 第一轮 15 → 第二轮 12 → 第三轮 6（出 78Z1qvnv 4.48 但 corr 0.76 被挡）→ 第四轮 12（出 4 个 A 档，全卡 corr 0.79-0.82，确认 liab 锚是相关源）→ 第五轮 12（g02 4.61 被 corr 0.79 挡；教训：**每提交一个新 alpha，相关性基线就抬高一次**）→ 第六轮 12（h03/h04 出货）→ 第七轮 8（Amihud 纯流动性家族补上第 5 个）。
- 68 候选 → 42 个模拟有效 → 9 个 A 档 → 5 个 corr<0.7 提交成功，0 个浪费提交配额。

## 关键经验（沉淀给后续每日挖矿）
1. **锚字段决定相关性**：liabilities_curr/assets 锚 + 反转 = corr 必撞 0.8；xrent/cash/opex/Amihud 锚能压到 0.6 上下。
2. **放量信号 `rank(volume/ts_mean(volume,60))` 是提分神器**：多次把 S+F 抬 0.3-0.5 且不显著推高相关性。
3. **Amihud 非流动性** `-ts_mean(abs(returns)/volume, N)` 是全新信号族，31 个老 alpha 里没人用过，T 还低（0.14-0.19）。
4. **decay 8→12 可把 T 压到 0.14 以下**，F 直接涨（zqYmRz0E T=0.139、gJQMxZoJ T=0.143）。
5. TUTORIAL 账号实测：无分析师/情绪数据集；`sum`/`ts_stddev`/`ts_max` 无权限；真实字段名 cashflow_op/enterprise_value/liabilities_curr/fnd6_* 等。
6. 每日纪律：testPeriod=P1Y 必开；预检 corr<0.7 再提交；同骨架一天最多吃 1-2 个，之后必撞墙。

## 明日继续挖的方向
- Amihud 族窗口/锚组合还有空间（60 日窗、×cap 变体）
- fnd6_xrent 族与不同反转窗（vwap/rev3）组合
- i05（amihud+opex, S+F=4.42, testS=3.09）质量很好，corr 0.84 被挡，明日基线刷新后可再试变体
