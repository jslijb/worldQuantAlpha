# 项目目录整理方案（2026-09-15）

> 角色：软件开发架构师｜目标：定框架 → 逐项审批 → 严格执行
> 状态：**待李工审批，尚未动任何文件**

---

## 一、现状体检

| 位置 | 条目数 | 体积 | 问题 |
|---|---|---|---|
| 根目录 | **221 个文件 + 10 个目录** | 1.7 MB | 186 个 .py 平铺，无任何分层 |
| docs/ | **431 个** | 40.2 MB | 混装 SDD 文档 + 223 个历史脚本 + PDF + 提取中间产物 |
| alpha_quality_analysis/ | **1185 个** | 5.0 MB | 1098 个 mined json 平铺（数据，合理） |
| _autologs/ | 33 个 | 4.4 MB | 运行日志 + 截图裁剪中间图 |
| `.codegraph/` `.codeartsdoer/` `.arts/` `.freebuff/` `.pytest_cache/` `__pycache__/` | 25 个 | 10.1 MB | 工具痕迹 / 缓存 |

**总计约 1900 个文件。** 其中：

| 类别 | 数量 | 说明 |
|---|---|---|
| **真正在跑的脚本** | **5 个** | submit_v2.py / probe_corr_service.py / mine_batch120.py / mine_batch121.py / auto_submit_loop.py（已停用） |
| 公共库 | 2 个 | utils.py / AlphaSimulator.py |
| 历史过程脚本 | **404 个** | 112 个 mine_batch + 150+ 个 submit + 57 个 hunt_round + 30 个 check/probe + 10 个 test/debug |
| 数据文件 | 1098 个 json | mined/ 挖矿结果（保留） |
| 文档 | 29 个 BRAIN_*.md + 7 个 SDD/项目 md | 知识资产（保留，需分类） |

**结论：99% 的代码是历史过程产物，核心生产链只有 7 个文件。**

---

## 二、目标目录框架

```
D:\Python\worldquant\
│
├── README.md                      【新建】项目总览 + 目录导航 + 快速开始
├── CLAUDE.md                      项目约定（AI 工具读，留在根）
├── brain_credentials.txt          凭据（脚本按相对路径读，留在根）
├── requirements.txt               【新建】依赖清单
├── .gitignore                     【新建】排除凭据/缓存/日志
│
├── src/                           ★ 生产代码（唯一，分层清晰）
│   ├── core/
│   │   ├── utils.py               公共登录/取字段库
│   │   └── AlphaSimulator.py      并发模拟管理器
│   ├── submit/
│   │   ├── submit_v2.py           ★ 当前提交器
│   │   └── probe_corr_service.py  ★ corr 服务双探针
│   ├── mine/
│   │   ├── mine_batch120.py       ★ 论文16 首轮
│   │   └── mine_batch121.py       ★ 论文16 变体批
│   └── ops/
│       └── auto_submit_loop.py    自动循环（已停用，留档）
│
├── data/                          ★ 数据（沿用原名，避免断路径）
│   └── alpha_quality_analysis/    （保持不动）
│       ├── SUBMITTED_LEDGER.csv   ★ 台账，唯一事实源
│       ├── mined/                 1098 个挖矿结果 json
│       ├── *.json                 字段清单（fnd6/analyst4/operators...）
│       └── mined*_summary.csv     批次汇总
│
├── docs/
│   ├── project/                   API.md / RANKING_GOALS.md / spec / design / tasks
│   ├── research/                  BRAIN_Research16 / UTR / 球队硬币复盘 / 配方总表
│   ├── study/                     BRAIN_零基础* / 备考* / 考试笔记
│   └── reference/                 官方 PDF（6 个）+ images/
│
├── archive/                       ★ 历史归档（不删，隔离但可追溯）
│   ├── mining/                    110 个 mine_batch*.py（除 120/121）
│   ├── submit/                    150+ 个 submit_*.py（除 submit_v2）
│   ├── hunt/                      57 个 hunt_round*.py（早期轮次）
│   ├── checks/                    30 个 check_/query_/verify_/probe_ 脚本
│   ├── tests/                     10 个 *_test.py / debug*.py
│   └── logs/                      60 个 hunt_log*.txt / *_log.txt
│
└── _autologs/                     运行日志（临时，保留）
```

---

## 三、文件去向映射

| 现有位置/文件 | 数量 | 去向 | 理由 |
|---|---|---|---|
| 根 `mine_batch*.py`（除 120/121） | 110 | `archive/mining/` | 历史批次，表达式已存 mined json |
| 根 `mine_batch120/121.py` | 2 | `src/mine/` | 当前活跃 |
| 根 `submit_*.py`（除 submit_v2） | 28 | `archive/submit/` | 一次性脚本，已失效 |
| 根 `submit_v2.py` | 1 | `src/submit/` | 当前活跃 |
| 根 `probe_corr_service.py` | 1 | `src/submit/` | 当前活跃 |
| 根 `auto_submit_loop.py` | 1 | `src/ops/` | 已停用，留档 |
| 根 `utils.py` / `AlphaSimulator.py` | 2 | `src/core/` | 公共库 |
| 根 `alpha_hunter.py` / `world4.py` / `mine_submit.py` / `mine_probe.py` | 4 | `archive/hunt/` | 初代挖掘器，已被取代 |
| 根 `*_test.py` / `debug*.py` / `quick_one` / `simple_ratios` | 10 | `archive/tests/` | 早期调试 |
| 根 `daily_*` / `final_*` / `find_and_submit_*` | 6 | `archive/submit/` | 初代提交器 |
| 根 `check_*` / `*probe*` / `corr_*` / `poll_*` / `scout_*` / `recheck_*` | 12 | `archive/checks/` | 一次性核对脚本 |
| 根 `fetch_/extract_/ocr_/render_/summarize_/calc_fitness` | 7 | `archive/tools/` | 一次性工具 |
| 根 `alpha_quality_analysis.py` | 1 | `archive/tools/` | 被 build_report.py 取代 |
| 根 29 个 `BRAIN_*.md` | 29 | `docs/research/` 或 `docs/study/` | 按性质二分 |
| 根 `CLAUDE.md` / `WQ_BRAIN_API.md` / `WQ_RANKING_GOALS.md` | 3 | 根留 CLAUDE.md，其余 → `docs/project/` | 工具固定读 CLAUDE.md |
| 根 `fnd6_cold.txt` / `w60_sum.txt` / `w61_sum.txt` | 3 | `archive/logs/` | 过程产物 |
| docs/ 223 个 .py | 223 | `archive/`（按族细分） | **已跑不通**（`from utils import` 但 utils 不在 docs/） |
| docs/ 50+ `hunt_log*.txt` + 各类 `*_log.txt` | 60 | `archive/logs/` | 历史日志 |
| docs/ 6 个 SDD md（spec/design/tasks/pitfalls/methodology/README） | 6 | `docs/project/` | 项目文档 |
| docs/ `idea_regret_factor.md` / `worldQuant 考试笔记.txt` | 2 | `docs/research/` / `docs/study/` | 分类归位 |
| docs/ `fundamental2_fields.csv` / `fundamental6_fields.csv` | 2 | `data/alpha_quality_analysis/` | 字段清单属数据 |
| docs/ 6 个 PDF + `images/`(43) | 49 | `docs/reference/` | 官方参考资料 |
| `_autologs/*.log` `*.json` `*.txt` | — | `_autologs/` 保留 | 运行日志，本就在正确位置 |

---

## 四、删除清单（分级，逐项理由）

### A 类：纯垃圾 / 死产物 —— 建议直接删

| 项 | 体积 | 理由 |
|---|---|---|
| `__pycache__/` | 192 KB | Python 字节码缓存，含已归档脚本的 pyc |
| `.pytest_cache/` | 极小 | pytest 缓存（8/31，测试脚本已归档） |
| `.codegraph/` | **10.1 MB** | 代码图谱 db + `daemon.pid`（8/28 启动，进程早已不在） |
| `alpha_list_pending_simulated.csv` | **623 KB** | 8/27 的待模拟队列，早已过期，AlphaSimulator 的历史输入 |
| `alpha_quality_analysis/batch25.log` | 0 字节 | 空文件 |
| `alpha_quality_analysis/.corr_block_cache_0912.txt` `.corr_block_cache_0913.txt` | 1.3 KB | 过期 corr 阻塞缓存 |
| `docs/pdf_pages/` `pdf_text/` `ocr_output/` | 15 个 | PDF 提取中间产物（原始 PDF 保留即可） |
| `alpha_quality_analysis/fig_*.png`（8 张）+ `QUALITY_REPORT.html` | 470 KB | 9/8 旧图表；md 版报告保留 |

### B 类：重复/失效脚本 —— 建议归档（不删），可追溯

见第三节映射表，共 **404 个 py**。**不直接删**，理由：这些脚本里的表达式设计和参数组合是研究过程记录，删除后无法复现"当时怎么想的"。

### C 类：需你判断（涉及其他工具 / 其他项目）

| 项 | 说明 |
|---|---|
| `.codeartsdoer/` | 华为 CodeArts Doer 的 agent/skills/mcp 配置。**如果你还在用这个工具就保留** |
| `.arts/` | 某 IDE 的 settings.json，来源不明 |
| `.freebuff/` | 仅一个 project-id 文件，来源不明 |
| 定时任务 `automation-1787747108480`「联想SRC方法论持续检索」 | **内容不属于本项目**（WorldQuant），每天 10:00 跑。建议迁走或停用 |

### D 类：必须保留

台账 CSV、mined/ 全部 json、字段清单 json、29 个 BRAIN_*.md、SDD 文档、官方 PDF、images/、`.workbuddy/`（记忆与自动化数据）、`_autologs/`。

---

## 五、执行顺序（审批后按此走）

| 步 | 动作 | 风险 |
|---|---|---|
| **0** | **全量备份**：整个项目复制到 `D:\Python\worldquant_backup_20260915\` | 无（纯复制） |
| 1 | 删 A 类垃圾 | 低（全是可重建/已过期） |
| 2 | 建 `src/` `archive/` `docs/` 子目录骨架 | 无 |
| 3 | 移动历史脚本 → `archive/`（按族） | 低（已跑不通的居多） |
| 4 | 移动文档 → `docs/`（按研究/学习/项目/参考） | 低 |
| 5 | 移动活跃脚本 → `src/`，**每个脚本顶部加 2 行根路径自定位** | **中**（需验证） |
| 6 | **更新 2 个定时任务 prompt 里的脚本路径** | 中 |
| 7 | 验证：跑 `probe_corr_service.py` 输出正常 = 路径改造成功 | — |
| 8 | 写 README.md + .gitignore + requirements.txt | 无 |

**第 5 步的路径改造方式**（每个活跃脚本顶部加）：

```python
import os as _os, pathlib as _pl
_os.chdir(_pl.Path(__file__).resolve().parents[1])   # 锁定项目根，相对路径不变
```

这样脚本放进 `src/` 后，`alpha_quality_analysis/mined`、`brain_credentials.txt` 等相对路径**全部照常工作**，无需改任何常量。

**降级选项**：若你认为改脚本风险高，第 5/6 步可跳过——活跃脚本留在根目录，根目录仍有约 10 个文件（vs 现在 221 个），整洁度已达 95%。**我推荐先做降级版，跑几天稳定后再做完整版。**

---

## 六、需要你现在点头的 3 件事

| # | 事项 | 我的建议 |
|---|---|---|
| 1 | **是否执行阶段 1**（备份 + 删 A 类 + 归档 B 类 + 建框架） | 建议执行。不动活跃脚本，零风险 |
| 2 | **活跃脚本是否移入 `src/`**（含改脚本 + 改 2 个定时任务） | 建议**先不做**，等阶段 1 稳定后再定 |
| 3 | **C 类怎么处理**（.codeartsdoer / .arts / .freebuff / 联想SRC 定时任务） | 需你说明是否还在用 |

---

## 七、附：本次未做的事

- 未删除、未移动、未重命名任何文件
- 未修改台账、脚本、定时任务
- 本次只新增 3 个只读扫描脚本（`_autologs/scan_project.py`、`classify_scripts.py`、连同既有 `probe_corr_service.py`）
