# 2026-09-17 工作日志

## 09:44 batch175 收尾判决（昨夜 0/34）+ decay 扫描开轴

### 背景
- 昨日关机时 batch175 挖矿阶段其实已跑完（39 变体全落盘，35 个质量达标），只差 corr 判决。
- 今晨补判决：`auto_submit_passers.py 'x175_*' --limit 20` → **0/34，全部撞墙**。
- 中途 Bash 工具 PortableGit PATH 损坏（ls/grep/tail 全 command not found，exit 127）误杀首个判决任务；**李工明确：本机只有 PowerShell，以后一律 PowerShell**（已写入用户级 MEMORY.md）。

### x175 判决的两个硬结论
1. **昨日的赢家 `gJbkQ31Q`（0.5999 入池）今天自己成了头号撞点**——34 条里 12 条撞它（0.75~0.998）。昨天验证的「隔夜加重+日内腿」配方进池即成新墙，"提交后必须重算池子"再次实证。
2. 未挂那两条腿的候选全卡 **0.689~0.702**（最近 2rw8Ap9x 0.6890，差 0.004）；平台 ≈ 本地 +0.006~0.017，全都没戏。**腿稀释矿脉挖干。**

### 补救：decay 扫描（batch176，从未测过的参数层维度）
- 假设：池子 79 条全 decay=10，PnL 时间轮廓同质；decay 拉到 22/32/44 平滑 PnL → 与池子相关性下降。
- 对 8 条擦线候选（0.689~0.702）× decay {22,32,44} = 24 变体。产出 `x176_{cid}_d{decay}.json`。
- 判决线不变：本地 ≤0.685 才提交。

### 台账状态（昨夜收盘）
- 80 行 / 79 唯一；美东 09-16 = 18 条；Super Alpha 距 100 差 21。

---

## 10:50 | 上午四连批全部收口：本地可及空间实测见底

| 批次 | 内容 | 结果 | 结论 |
|---|---|---|---|
| x175 | 34 条近门槛变体判决 | **0/34** | gJbkQ31Q 进池即成头号撞点（12 条撞它）；其余卡 0.689~0.702 |
| x176 | decay 22/32/44 扫描（8 候选×3） | **0/12** | corr 不降反升 +0.02~0.03（0.689→0.711~0.723）；**decay 平滑推向池子共同慢分量** |
| f177 | analyst4 二次开采（PEAD 8 + 0阶30 + 覆盖数4） | **0 种子** | PEAD 全灭（SF 0.04~1.15）；最强 `anl4_qfv4_eps_number` SF 2.96 带 LOW_SUB_UNIVERSE；**analyst4 彻底关闭** |
| x178 | 逐腿 group_rank→group_zscore/group_neutralize（8 候选×2） | **0/14** | 质量直接摧毁（最好 SF 3.46<4.0）；GN 略优于 ZS 但都不达标 |

**当日产出：0 条。池 79，距 Super Alpha 差 21。历史欠账 0（今日目标 8 无本地路径）。**

### ★ 根因（不是执行问题，是资源上限）
- 账号可及 14 个数据集已全部实测绘图：只有 fundamental6 / fundamental2 / pv1 能撑 SF≥4 主腿；
- 这三个数据集能拼出的互不相关几何 ≈ 已入池的 79 条，**边际信息量 ≈ 0**（560 条库存筛查 + 四轮补救全部 ≥0.689 实证）；
- 100 条 Super Alpha 需要约 100 个互相关 <0.7 的信号，**当前数据权限下数学上够不着**。

### 补救（需要李工在平台操作）
1. **申请多区域权限（最高价值）**：换区域是唯一能让现有全部配方「零自相关重启」的杠杆（同表达式在 EUR/ASI/JPN 的池子与 USA 池互不相干）。当前 ASI/EUR/GLB/HKG/JPN 全部 400。需在平台确认 Consultant 是否可申请多区域 simulation 权限。
2. **申请新数据集**：同类别更多数据（新闻/情绪/期权已证弱）帮助有限；优先问平台哪些**未开放数据集**可申请。
3. 在拿到新权限前，本地不再空转批次（连续 54 个变体 0 命中，继续烧额度无意义）。

---

## 晚轮（北京 19:33 起，李工令：开始，目标 5 条高质量）
- **自动化已恢复**：`WQ每日Alpha挖矿提交`（07ac2d4a）PAUSED→ACTIVE（每日 16:00 触发），提示词同步成当前流水线口径（auto_submit_passers / submit_v3 / 本地 corr 0.685 缓冲线 / 判死 15 项勿重试）。另两个旧 WQ 自动化（补提交 hourly、裁决复查）仍是 PAUSED，已过时待清理。
- **截图读数**：Rank 208（632→208 大跳）、Total Score 0.53、IS Score 8150、Uniqueness 0.64、Days of Submission 21。
- **当晚开轴 x179**：x175 判决 0/34 后，30 条 SUBINDUSTRY 达标候选从未换过 SECTOR——`mine_neut_convert.py --cids <26条> --neuts SECTOR --pfx x179`。首批 2 条完成即质量 PASS（SF 4.65~5.15，tS 2.39~2.66），待 corr 判决。
- **腿库扩容 b180**：`x180_leg_int`（日内腿 `group_rank(-ts_rank(close/open-1,20),subindustry)` 单腿 S=1.71）已入库；`x180_leg_cov`（分析师覆盖数变化）在跑。leg_lab.py 已加 ALIAS 映射 + P_int20/M_cov 两腿。
- **★ WorkBuddy PowerShell 工具本会话故障**：前台 stdout 捕获为空（连 Write-Output 都空）、后台任务 2 分 01 秒硬超时被杀（exit 1 无输出）。**绕过范式**：①结果一律写文件再 Read（python -c 内 open().write() 或 `| Out-File -Encoding utf8`；`*>` 重定向是 UTF-16，Read 读不了）②长任务用 `Start-Process -NoNewWindow -PassThru -RedirectStandardOutput/-RedirectStandardError` 分离进程，轮询日志文件。bash 工具仍是坏的（PATH 损坏），勿用。
- **我的手误两次**：`sys.argv[... + 1)]` 括号错（b172/b175）、`import os as _os` 后用 `os.path`（b180）——写完必跑 ast.parse 自检已固化。

### x179 判决（北京 20:10）：0/19，但拿到投影杠杆的适用边界
- x175 的 19 条 SUB 达标稀释组合全转 SECTOR 后质量全保（SF 3.96~5.15，tS 最高 2.66），但 corr **0.7142~0.9479 全撞墙**，且**比 SUB 版（base 0.689~0.74）普遍更高**。
- **★ 机制结论**：中性化投影的 −0.14~−0.27 下移**只对「共享分量 = SUBINDUSTRY 组内基本面腿」的候选有效**（投影把这个分量投影掉了）。x175 稀释组合的共享分量是 OVN/INT/PV 价量腿——这些分量**跨中性化存活**，投影掉基本面差异后反而与昨天的稀释赢家（gJbkQ31Q=INT+OVN 腿、58znv2Y1、akbjdK26、pwRwWoJ3）更像。撞点分布：gJbkQ31Q×8、pwRwWoJ3×5、akbjdK26×3。
- **推论**：要破墙，稀释腿必须换池里零条且与已入池稀释腿正交的几何 → **COV（分析师覆盖数变化 `anl4_fs_detail_estimate_1qf_v4_nd_epsr_number` ts_av_diff45，单腿 S=1.36、T=5.3%）**，已入库 `x180_leg_cov`(QPbZVddG) / `x180_leg_int`(XgbQZkp1)。
- leg_lab 已扩容：ALIAS 映射 + `P_int20`（日内）/`M_cov`（覆盖数）两腿，search `--k --q --max-corr 0.55 --min-s 2.2 --diverse` 全库搜索中。
- batch181 执行器就绪（读 search --emit 的 json → 批量模拟 → 判决）。


## 0917 晚间：leg_lab 新腿组合链路打通，当日 0→2
- **x180 补腿**：M_cov（分析师 EPS 覆盖机构数变化 ts_av_diff,45）单腿 S=1.36 / 换手 5.3%，入库 leg_lab；P_int20 日内腿同批入库。
- **x179 全批判决 0/19**：SECTOR 投影对『稀释组合』无效——投影去掉组内基本面分量后，剩下的价量/日内分量反而更像 gJbkQ31Q。**中性化投影的适用边界最终敲定：只救『单锚基本面』近门槛（0.70~0.72），不救稀释组合。**
- **leg_lab 搜索一轮**（max-corr 0.55、diverse）：1.82 亿组合 → 9 条候选 → batch181 模拟：3 条质量达标 → 判决 **2 条入池**：levb0dQx（平台 0.5858）/ npdM3G7z（0.6452）；1YXQJRoX SF6.69 撞 pwRwWoJ3 0.89（pred 0.55 实际 0.89，低估再次验证）。
- 美东 09-17 当日 2 条；台账 82 行 / 81 唯一；距 Super Alpha 100 差 19。
- 搜索二轮进行中（max-corr 0.58、take 80，池子已含新对手）。
## 0917 深夜：目标 5 条打满（美东 09-17 = 5，台账 85 行/84 唯一）
- leg_lab 加 --with 强制锚开关（锚组合必须含指定腿），COV/M_tot/M_ni/M_liab/M_tfvce/M_txp/M_revt/M_opex/M_cashZ 九族逐锚扫完；M_g12 并行三路时 MemoryError 放弃（单腿也弱）。
- 后四条入池：kqo2oOXd(0.6396)/N1avK6zw(0.6806)/YPbGEV3q(0.6108)，加早间 levb0dQx/npdM3G7z，共 5 条全高质量。
- **pwRwWoJ3 磁铁效应**（8 次撞点）：SF 6+ 的组合全是引擎腿+无形资产腿重仓，注定撞它；能过线的是中等 SF(4.0~4.3)+独有锚组合。pred→real 偏移 +0.09~+0.34，与引擎腿占比正相关——预测只能筛、判决靠真值。
- 撞点分布：约 60% 撞 pwRwWoJ3，其余散布 vRjG1Jzw/gJbkQ31Q/kqo2oOXd 等（新入池者立变撞点）。
- batch183_prep 定向变体 24 条全灭（0 过线）——围绕赢家做变体=长得像赢家，堵死。
- Super Alpha 距 100 还差 16。
