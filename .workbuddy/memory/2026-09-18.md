- 面试数据篇答疑：李工焦虑没用过平台/答不出字段含义。纠正=靠流水线挖过36条Alpha+fundamental6/volume字段，概念熟UI不熟；面试不考英文翻译，考类型/选型/经济含义。产出 docs/exam/面试_字段与数据基础速记.md (Matrix-Vector-Group+数据家族+6tips选型+6自测题)。

---

## 16:00 自动化轮（w196~w198 三批判决：30 候选 / 17 质量达标 / 0 提交）

### 执行流水
1. **库存核对**：w182~w195 全部已判（judge 日志齐全），库存清零。台账 85 行 / 84 唯一（YP57jVzo 为 0909 旧重复行，不清理）。
2. **w196 批（leg_lab 最后 7 个未扫锚）**：M_debt/M_tstk/M_intc/M_accI/M_intI/M_cfoQ/M_lnoq 逐锚 search --max-corr 0.55 --min-s 2.2 --diverse → 19 组合 → mine_batch181 执行器模拟 → **8 条质量达标（SF 4.00~6.28，tS 全 ≥1.51）→ 0 过墙**。
3. **x196 中性化转换**：w196_11（corr 0.7219 最近门槛）→ SECTOR 3.82/MARKET 3.22 质量崩；w196_12（0.7497）→ SECTOR 5.08 / MARKET 4.29 达标但 corr 0.7904/0.7491 仍封死。**SECTOR 版 corr 反升 +0.04——第三次实证：投影救不了稀释组合（x179 边界结论再次成立）。**
4. **w197/w198 严格搜索（max-corr 0.50）**：12 锚全扫完，出 7 条（pred 0.49~0.50）→ 质量全达标（SF 4.68~6.01）→ **0 过墙，corr 0.79~0.87**。w196_11 的 0.7219 仍是今日最低。
5. 收尾：git 快照完成（84 文件，首次推送 502，重试 1 次成功）。

### ★ 结论：leg_lab 离线拼装轴正式关闭（当日完整实证，非外推）
- **12 个 M 锚全部扫毕**（0.55 与 0.50 双阈值），可及几何出尽。
- **pred→real 偏移完全不可控**：+0.05（w196_11）到 **+0.39**（w197_00 pred 0.49→real 0.864）。pred 0.50 区不比 0.55 区好。
- **今日最低 real corr 0.7219**（w196_11，撞 rY07MXgd），距 0.685 缓冲线差 0.037。
- **撞点已换成自己人**：今日头号撞点是 pwRwWoJ3（×5）、LLNL3qqm（×4）、vRjG1Jzw（×3）、YP57jVzo、O0N0R5NY、rY07MXgd、YPaPe0oo——0917 入池的 leg_lab 系赢家把库内「低重叠区」全部占住。**赢家进池即封死自己的矿脉，第 N 次实证。**
- 质量不是瓶颈（17/30 达标，SF 最高 6.28）；唯一瓶颈是 corr，且在当前 84 条池 + 可及 14 数据集下**无本地路径**。

### 数字账
- 本轮提交 **0**；台账 84 唯一；美东 09-18 = **0/5**；欠账 **5** 顺延（明日美东目标 = 5 + 5 = 10）。
- Super Alpha 距 100 差 **16**。

### 会话中断根因（李工问"继续会话为什么总是失败"）
- **Bash 工具 PortableGit PATH 损坏复发**：`tail/dirname command not found`、exit 127 且无具体报错（当日 19:31 现场捕获）——命令静默失败，宿主只显示"会话中断"。
- 16:00 自动化在 17:24 左右等模拟输出时被宿主挂起 3 小时，无错误回传；本轮全部脚本有断点续跑 + 日志落 `_autologs/`，进度无损。
- **规避范式（已全程执行）**：终端一律 PowerShell；PowerShell 前台 stdout 捕获为空时用 python 落文件再 Read；`*>` 重定向是 UTF-16，Read 前需转 UTF-8；后台长任务轮询日志文件而非 sleep。

### 待李工平台侧操作（唯一解锁路径）
多区域权限 / 新数据集权限申请——本地 14 个可及数据集已全部测绘完毕（含今日 12 锚收官），无本地增量空间。

---

## 23:29 李工追问"旧因子修改再提交"——补测试收口（全路径实测关闭）

李工质疑"所有数据试过"的结论，提出两条路：①他提供思路 ②旧低质量因子修改再提交。当轮补实测：

1. **456 积压最近门槛的 w59_b（le8EAJLO，本地 corr 0.6934）0916 已提交入池**（selfCorr 0.6997 < 0.7 直通）——积压最优解早已吃掉。剩余积压 corr 下限 0.7387 → 平台 ≈0.745+，直通死。
2. **x163 补判决**：w27lnoqxrent 的 MARKET 版（vRrwaX7z，SF4.15/tS1.28，16 条 MARKET 转换里唯一质量达标者）corr 0.7398 撞 YPbLZK2W（0916 MARKET 转换赢家）→ 死。
3. **x199 补转换**：w114_m SECTOR 版 SF 3.79 质量崩（<4.0）、MARKET 版 2.74 崩；w59_b 被工具正确识别"已入池"跳过。SECTOR 版对积压老骨架同样保不住质量。
4. **结论（实测，非外推）**："旧因子修改再提交"三条变体全部关闭：直接重提（corr 死）、中性化转换（质量崩或撞自家 MARKET 赢家）、低质量升级（质量与相关同源，加腿提 SF 即涨 corr）。
5. 李工方向①（提供思路）是活的：历史上三次破墙（/cap 缩放、市值分桶、评级偏离度轴）全部来自外来思路移植。要的形状 = 新研报/新数理方法，不是"换字段换窗"（判死表全覆盖）。

---

## 23:38 李工批评"陷入盲区、自己误导自己"——当轮诊断出四个硬伤

李工原话：「经常陷入自己的盲区，自己误导自己，出了问题不想办法解决，不换思路，不是说别人的系统有故障就是说数据不够」。当轮用数据自查，**坐实四个盲区**：

### 盲区① 拿着过期的标定常数开车
用今日 15 个 (pred, real) 配对观测实测：偏移 mean **+0.29**（min +0.1755 / max +0.3906），而记忆里的标定是"+0.05~0.19 均值 +0.12"——误差 regime 已漂移 2.5 倍，我还在用旧常数调搜索阈值。**更致命：corr(delta, pred) = -0.69，pred 越低偏移越大**（pred 0.49 → real 0.86），"压低 pred 阈值"这个搜索方向本身是反的。pred 0.49~0.55 区间内 real 与 pred 呈负斜率 ≈ -2.6 → **pred 在当前池子 regime 下已失去信息量，调阈值救不了**。
- 分析脚本：`_autologs/analyze_pred_real.py`（日志是 UTF-8→GBK→UTF-16 双重乱码，「撞」字会吃掉对手 id 首字母，解析只用 ASCII 部分）。

### 盲区② 根因诊断出来了却没写全：共享腿权重占比决定 real corr
对撞点表达式逐腿对比（w197_00 vs LLNL3qqm）：共享腿（cfo/ev45 1.0 + v90 2.0 + tr20 2.0）= **4.0/10.5 ≈ 38% 权重**，其中 PV 腿被搜索器加到 wp=2.0（0916 赢家 PV 只有 1.0~1.5）。机制假设：模拟器重整化把组合信号向共享主导分量集中 → 共享权重 ≥35% 的组合真实 corr 必然 0.8+，与腿怎么分组无关。
- **w159 实证收口**：batch159 的 industry 分组 8 条全部质量达标（SF 5.43~5.94），**7 条从未判决**（当天判死 ⑩"空间未打开"就搁置了）。当轮补判决 0/7（0.84~0.996，3 条撞自家兄弟 O0N0R5NY，最好的 w159_00 撞 9qjqm3Q9 到 0.879）——**换分组救不了共享权重主导的组合**。

### 盲区③ leg_lab 静默丢池成员的真 bug
`pool[_pid] = _v` 在 PnL 拉取失败时静默跳过：**rY07MXgd 的 PnL 从未缓存成功**，w196 搜索时它不在池子里，而 w196_11 实测 corr 0.7219 撞的恰好就是它。工具该报警的地方静默通过。（红线"只追加"约束下未改 leg_lab.py，bug 记录在案。）

### 盲区④ 库里有活没干完就宣布"空间见底"
w159 的 7 条判决是当轮才补的。宣布"无本地路径"之前没有先清点库存里未判决的达标候选——这是"结论先行"。

### 剩余的真实杠杆（不需要新权限）
**池子 86 条里没有任何一条以日内/隔夜几何为主腿**（gJbkQ31Q 里日内腿仅 1.25/8.75 ≈ 14% 权重，仍是标准骨架主导）。球队硬币帖移植招①「隔夜/日内拆分」只用过这一小口。
- **明日探针**：新写追加式驱动脚本（不改 leg_lab.py），生成「日内腿主导（1.5~2.0 权）+ 引擎/PV 腿降权（0.5~0.75）」的组合批，模拟后用真实 PnL 判决。风险提示：记忆有"新信号轴顶替 PV 腿后 tS 崩"的实录，但那批是 option/news/social 轴；日内腿属价量族（open/close 派生），且 gJbkQ31Q 证明其可入池。
- 次级杠杆：用今晚 15 点观测拟合共享权重→偏移的修正模型，恢复 leg_lab 预测力（n=15 偏小，需再攒观测）。

### 数字账（当日终）
- 提交 0；台账 84 唯一；美东 09-18 = 0/5；欠账 5；Super Alpha 差 16。
- 当日总判决量 33（新挖 30 + 补测 3）+ w159 补判决 7 = **40 条**，质量达标 24，过 corr 线 0。


