# 相关性墙破法（实战记录）

> 来源：项目 0910–0915 的实测记录 + 平台规则
> 这是本项目唯一真正的瓶颈——**质量关（S+F≥4.0）能挖出 460 条，但相关性关卡住 90%**

---

## 一、规则本身

| 项 | 内容 |
|---|---|
| 触发条件 | `selfCorrelation ≥ 0.7` 触发 Production Correlation 测试 |
| 通过条件 | **max corr < 0.7** **或** Sharpe 比相关 alpha 高 **10%** |
| 豁免线 | `1.10 × max(所有 corr≥0.7 对手的 Sharpe)`，候选 S 达线即可提交 |
| 计算对象 | **当前已提交池**（实时计算，非历史快照） |

---

## 二、最关键的一条性质：相关性是动态的

**实测**：同一个表达式，预检 corr 从 **0.616 → 0.9879**，只因中途提交了两个结构相似的因子。

**机理**：self-corr 是对"当前已提交池"实时算的 → 每提交一个因子，结构相似的兄弟因子 corr 立刻被顶高。

**三条推论**：
1. 提交后必须对剩余池**重新预检**，旧结论全部作废
2. 同构候选**严禁扎堆**提交
3. 必须常备多套**结构不同**的 PNL 储备

---

## 三、有效的破墙手法（按时间，全部实证）

| 手法 | 实例 | corr | 关键动作 |
|---|---|---|---|
| 五腿稀释 | w60_h | 0.6548 | 加腿摊薄共享成分 |
| 独有分析师锚 | w64_h（GI 锚） | 0.6605 | 锚必须**独有**（别人没用过的字段） |
| 换价量腿 | w68_b（PVD5 = ts_delta5 + vol90） | 0.6948 | **同时**换锚 + 换 PV 腿 |
| 双独有 analyst4 锚 + 换 PV 腿 | w77_a（CFI + SGA） | **0.6598** | ⚠️ 一个配方只能吃一口，同骨架复制必互撞（batch78 全灭 0.82–0.95） |
| **腿系数加权改 PNL 构成** | w85_a | **0.754 → 0.6803** | 独有 fnd2 锚**全权** + 共享 PV 尾部**降权 0.5**（0.3 质量崩，0.5 是甜点） |
| **共享腿降权 0.5（复现 w85_a 手法）** | w122_a `akLp3pzW` | **0.8036**（未达 0.70） | 降权幅度不够；靠**豁免**入池（S=2.78 / 边距极薄）——详见第八节 |
| 7 腿配方（Research16 移植） | w121_d `3q9NMgk6` | 待检 | 3 fnd6 锚 + 引擎腿 + PV + 分析师评级腿 |

### 共性
> **必须含"独有成分腿"** —— 即结构上别人复制不了的腿。

### 双轮换法则
> **锚腿（冷门字段）+ 价量腿同时换。**
> 只换一个没用（单锚嫁接稀释不够，batch74 corr 0.78–0.89）。
> 避开最拥挤的 `ts_delta(close,2)` + `volume/ts_mean(volume,60)`。

---

## 四、拥挤腿清单（骨架已饱和）

这些腿在已提交池里高度共享，用一个少一个：

```
CASH45（ts_av_diff(cash/assets,45)）
CFEV45（ts_av_diff(cashflow_op/enterprise_value,45)）
PV（-ts_delta(close,2)）
TXTUB（fnd6_txtubxintis）
XRENT（fnd6_xrent）
PTPR
```

骨架饱和后 corr 下限 **0.75–0.93**。

⚠️ 矛盾点：`CASH45`/`CFEV45`/`XRENT` 同时是**质量最高的腿**（全过率 77.3%/73.9%/71.6%）——
**这就是"质量越高越难提交"的根源**：好东西大家都用，用完就撞。

---

## 五、已证伪的破墙尝试（别再试）

| 尝试 | 结果 |
|---|---|
| 存量 mined 池当候选 | 234 个达标候选**全灭**（20 结构代表 corr 0.73–0.99，热对手 S=2.72–3.25） |
| 单锚嫁接（在冠军骨架上换一个锚） | corr 0.78–0.89，稀释不够 |
| 全新 fnd6 腿当**主腿** | 质量全灭（SF 1.87–3.67）——cash45/cfoev45 是质量引擎不能丢 |
| 纯复制冠军骨架换锚 | w69 系 corr 0.78–0.88 |
| analyst4 离散度/估值轴 | corr 0.91（但**评级偏离度轴可用**，见 Research16） |
| 强 `trade_when` 门控 | 伤质量 |
| TOP1000/500 换池 | S 崩且 testS 全 <1.25 |
| truncation 放开 / INDUSTRY、SECTOR 中性化单用 / PV 腿微调 / D0 独立性 | 全部无效 |

---

## 六、没试过的杠杆（下一个方向）

1. **不同数理方法处理同一数据**（球队硬币帖元教训）——
   贴主说"优秀量价因子 PnL 殊途同归"（行业中性化公共库 corr≈0.8），这正是我们 PV 腿怎么换都撞墙的原因。
   破局 = 叠加其他数据（已做）+ **换一种数学处理方式处理同一批数据**（未做）。
   
2. **Slow + Fast Factors 中性化** —— 从未测试过（`INDUSTRY` 试过，转 w68_b corr 0.83）。

3. **未开发数据集** —— `news12`（875 字段）、`model51`、`pv13`（165）、`option8`。
   新数据集的字段天然"独有"，是压 corr 最直接的路。
   A 档记录里已出现 `news_open_gap`(7次)、`news_short_interest`(4次)、`dividend`(5)、`sharesout`(5) —— 线索。

4. **条件符号切换**（`ts_std_dev(returns,20) < group_mean(ts_std_dev(returns,20),1,market) ? -ts_delta(close,10) : ts_delta(close,10)`）
   —— 用截面比较翻转符号 = **结构级差异**，不是参数级差异。`src/archive/expr_library.py` 的 `PY_ONLY` 里有这条，**从未模拟过**。

---

## 七、提交流程（含并发注意）

1. 质量闸门：S+F ≥ 4.0 且 testS ≥ 1.25 且 checks 无 FAIL
2. corr 预检：`GET /alphas/{id}/correlations/self`
3. 判定：max corr < 0.7 → 直通；否则算豁免线 = 1.10 × max(所有 corr≥0.7 对手的 S)，候选 S 达线也可提交
4. `POST /alphas/{id}/submit` → `GET /alphas/{id}` 核 `status == ACTIVE` → 追加台账

**并发警告**：同账号并发提交会互堵（0914 实测 w95_a/w96_a/w96_b/w101_e 四个 POST 201 后永不裁决，w101_f 秒过）→ **必须串行提交**。

---

## 八、拿到"相关判决"的正确入口（0915 实测，修正第 2 步）

### 8.1 事实

第 2 步用 corr 端点取数的做法，目前在本人账号上取不到数：

| 观测 | 结果 |
|---|---|
| 缓存探针 `E5vj5YdL` 连续轮询 **51 次 / 90 秒** | `200 + Retry-After:1.0 + bodylen=0`，始终无 records |
| 全新探针 `KPOAOXEE`（w112_a）同上 | 同样 90 秒无 records |
| `corr_only.py` 对 `883wqwVX` / `KPO1elgN` | 各 180 秒预算耗尽，**双 TIMEOUT** |
| `corr_sweep_once.py` 一次性快扫（每条 1 请求） | `_autologs/corr_ready_0915.txt` **为空**—— 一条"已算好"的都没有 |
| 同期**全量观测**：有没有 429？ | **没有**。全程零 429 → **不是限流封禁** |

**结论**：`200 + Retry-After + 空 body` 的语义**确实是"平台正在现算"、不是故障**（这一点 0915 的修正方向对），
但当前的实情是 **相关计算在账号侧排队积压**，不是 3~4 次轮询就能出。90~180 秒量级都拿不到。

### 8.2 能确凿拿到 corr 数值的唯一路径：提交成功后读 `is.selfCorrelation`

实测对照：

| alpha | 动作 | `GET /alphas/{id}` | `is.selfCorrelation` |
|---|---|---|---|
| `akLp3pzW`（w122_a，0.5 降权版） | 盲提交 POST → ACTIVE | status=**ACTIVE / stage=OS** | **0.8036** ← 数值只能这样拿到 |
| `883wqwVX`（w122_b，0.35 降权版） | 盲提交 POST 200/201 | 提交状态轮询 **59 轮（~8 分钟）status 全为空** → 未裁决 → 仍 UNSUBMITTED | 无 |

⚠️ **一条重要的反向修正（本轮差点写错，已用对照验证）**：
`is.checks` 里的 `SELF_CORRELATION = PENDING` **不是"提交触发了计算"的信号**——
它是**所有未提交 alpha 的默认占位符**。对照 5 条**从未触碰过**的候选，全部一样：

```
O0r3epMg / le8mxdn7 / 3q9NMgk6 / 6XrkW6R5 / KPOAOXEE   → 全部 checks=8 条，SELF_CORRELATION=PENDING
```

所以 **不能**用它判断"提交是否把计算推上去了"，也不能用它当"服务在算"的证据。
真正有判别力的是：**alpha 变成 ACTIVE 之后**，`is.selfCorrelation` 才有值。

**由此得到的操作结论**：
- 想要 corr 数值 → 先让 alpha 提交成功，再读 `is.selfCorrelation`；
- 未提交状态下，除 `correlations/self` 外**没有别的取数入口**，而该端点当前取不到数；
- ⛔ **不要再靠 `correlations/self` 反复打**：一条候选动辄上百次请求，纯粹加剧本已积压的相关计算队列
  （0915 本轮实测：探针 ~102 次 + corr_only ~276 次，之后相关端点始终无 records）。

### 8.3 盲提交（`src/submit/blind_submit.py`）的实战表现

| 案例 | 结果 |
|---|---|
| `akLp3pzW`（另一会话执行） | 成功 → ACTIVE / OS / corr 0.8036 |
| `883wqwVX`（本轮执行） | POST 返回 200/201，但**提交状态端点 59 轮全为空**，8 分钟后仍 UNSUBMITTED → **复现 0914「POST 后永不裁决」的互堵模式** |

**纪律**：`blind_submit.py` 一次只提一个，**出现 PENDING 立即停手做下一个**（脚本已内置该逻辑），否则会把整个提交管道堵死。

### 8.4 w122「共享腿降权」实战结果（0915）

| 变体 | id | S / F / tS | 降权系数 | 结果 |
|---|---|---|---|---|
| w122_a | `akLp3pzW` | 2.78 / 2.22 / 2.46 | **0.5** | 已提交，实测 **corr = 0.8036**，走**豁免**过（不是直通） |
| w122_b | `883wqwVX` | 2.63 / 2.24 / 2.24 | 0.35 | 盲提交未裁决，仍 UNSUBMITTED |
| w122_c | `KPO1elgN` | 2.48 / 2.15 / 2.03 | 0.25 | 未触发（按"一次只提一个 + PENDING 停手"纪律未提交） |

**判读**：降权 **0.5 不足以把 corr 压到 0.70 以下**（实测 0.8036 > 0.70）。w85_a 当年靠 0.5 降权从 0.754 直降到 0.6803 能直通，
这次同一手法只到 0.8036 —— 说明**降权幅度不是普适杠杆**，效果取决于共享腿在 PNL 里的实际占比。
a 之所以仍能入池，靠的是**豁免**（S=2.78 ≥ 1.10 × 热对手 S≈2.53），边距极薄。
⚠️ a 入池后，b/c/d/e/j/k 等同骨架兄弟的 corr 会被立刻顶高 → **按第二节"相关性是动态的"，它们的旧判断全部作废**。
（当前 w122_ 达标未提交 6 条：b 4.87 / c 4.63 / d 4.40 / e 4.40 / j 4.33 / k 5.02，括号内为 S+F）

**下一步方向**：降权继续往下压（0.35/0.25）本轮未能取到判决；
真破墙仍要回到**换更冷的锚**（`src/mine/mine_batch123.py`：fundamental2 里 alphaCount≈0 的字段）。

### 8.5 判决机制定案（0916 上午，推翻 8.2/8.3 的"排队"解读）

8.2/8.3 写于假象之中。真相：

1. **判决从来 4~9 秒就出**：`POST /alphas/{id}/submit`（201）→ 轮询 `GET /alphas/{id}/submit`：
   `200 + Retry-After + 空 body`（计算中，约 4 秒）→ **`403 + {"is":{"checks":[...]}}` = 判决书**。
2. **`blind_submit.py` 的 bug**：判决书顶层是 `is.checks`，没有 `status`/`stage` 键；脚本只认后者，
   把判决当空数据丢弃 → 空转 59 轮报"假 PENDING"。0914~0915 两天的"互堵/积压/服务不可用"叙事**全部由此而来**。
3. 判决书在端点有时效，过期 GET 返回 404——**重新 POST 即可再取判决**（58QPEeOM 实证，两次 POST 均被受理）。
4. alpha 对象的 `is.checks.SELF_CORRELATION` 恒为 PENDING（未提交时的默认占位符），**判决只出现在 submit 端点**。

**正确工具**：`src/submit/submit_v3.py`（解析 403 判决，裁决落 `SUBMIT_VERDICTS.csv`）+ `run_queue_v3.py`（串行队列）。
`correlations/self` 端点、blind_submit.py 均弃用。

### 8.6 0916 大判决日：9 条同骨架候选全灭（corr 全实测）

| 候选 | alpha_id | corr | 备注 |
|---|---|---|---|
| w122_b | 883wqwVX | **0.9919** | w122_a 入池后同骨架被顶到近乎 1（"相关性是动态的"实锤） |
| w121_d | 3q9NMgk6 | 0.8625 | 7 腿 Research16 配方，此前 corr 从未实测 |
| w126_h | LLNPbLx6 | 0.8420 | |
| w126_g | P02JjJOx | 0.8167 | |
| w125_h | 58QPEeOM | 0.8130 | 其余项全 PASS（S2.57/F2.21/子域1.56），纯死在 corr |
| w125_a | blRe65g6 | 0.7935 | |
| w124_e | 6XrkW6R5 | 0.7791 | |
| w125_f | GrdZWbvO | 0.7723 | |
| w125_c | Vk6AxE1b | 0.7367 | |
| w125_d | j23qYr8O | 0.7265 | 全场最低，仍差 0.0265 |

**结论**：w124~w126 的"三锚 + 共享腿 0.5 降权"整族封死——最低 0.7265 也没进直通区；
豁免线 = 1.10×热对手 S（w122_a 2.78 → 3.06；0mR2K6lr 3.45 → 3.80），本族 S 最高 2.78，**无人够得着**。
**唯一出路 = corr<0.70 直通**：真不同骨架，且冷锚必须先单锚验证有数据（见 `01_…` 第五/六节与 batch127 教训）。

---

## 九、相关文档

- `docs/methodology/01_骨架与配方/01_黄金骨架库.md` — 破墙前的质量骨架
- `docs/methodology/05_历史因子复盘/01_历史资产盘点.md` — 456 条未提交库存怎么排队
- `docs/research/Research16_分析师短期交易思路.md` — 评级偏离度轴（已验证可用的新轴）
