# 2026-09-19 工作日志

## 00:34~01:25 李工令"继续干"——日内轴破墙，w200_05 过墙入池

### 乱码修复
2026-09-18.md 52 行的 `?0.69`/`?2.6` 是写入时 U+2212 负号（−）变 `?` 所致，已改 ASCII 连字符。教训：**写 md 一律用 ASCII `-`，不用 Unicode 负号**。

### 破墙过程（诊断→配方的完整闭环，当晚见效）
1. **w200 日内主导批**（新生成器 `src/mine/mine_w200_intraday.py`）：主腿 P_int20（日内反转）1.5 + 2×fnd6 锚 0.75 + 引擎 0.5 可选，**零标准 PV 腿**，共享占比 0~0.14。8 条模拟 8/8 质量达标（SF 4.55~5.55）。
2. **判决：w200_05（3qXd5vg0）过墙提交**——本地 corr 0.6765、平台 selfCorr **0.6842 < 0.7 直通**，SF 4.74 / tS 3.00，dateSubmitted 2026-09-18T12:48 美东。**两日 0 提交后首破，台账 86。**
3. **偏移标定定律（本轮最大收获）**：pred→real 偏移随共享腿权重占比缩放——shared=0.14 → 偏移 +0.07~+0.11；shared=0 → **偏移 −0.03~+0.05**。低共享区 pred 可信，搜索可用 pred≤0.66 做硬过滤。
4. **w201 轴内扩展收口**：新腿 P_int10/40/60、P_int20I/C/V（换分组）单腿模拟——P_int10 最强（standalone S 2.02）但组合质量塌（16 条 SF 2.73~3.46 全 <4.0）；P_int20 族高 S 变体全部撞 3qXd5vg0（pred 0.78+）。隔夜族 standalone 为负（S −0.45~−0.96）弃用。
5. **w202 条件门控腿收口**（球队硬币帖招②）：C_int_vg（波动率门控）S 1.03 带 FAIL；C_int_gap/2s S 0.5 带 FAIL——扛不起主腿。
6. **日内轴宣告一口**：「一条轴一口」升级为完整实证——裸反转（吃掉）、窗口（弱）、换分组（弱）、条件门控（死）、隔夜（负）、动量（负）。

### 数字账
- **美东 09-18 = 1/5**（3qXd5vg0）；欠账 9（5 + 昨日 5 − 今日 1）；台账 86；Super Alpha 差 14。
- 当夜判决 8+16+4=28 候选、22 条模拟产出、1 条提交。

### 待续思路
- 生成器可复用：`mine_w200/w201_intraday.py` 模式 = 「新几何主腿 + 独有锚 + 低共享」，配 pred≤0.66 硬过滤 + shared≤0.10。
- 下一条待探几何：暂无显见候选；优先等李工研报/思路（移植流水线现成），或平台权限。
- leg_lab 静默丢池成员 bug（rY07MXgd）仍未修（红线"只追加"），w200/w201 生成器已内置"拉取失败报警"。

## 11:58~12:30 账目核对 + 项目清理讨论 + w203 开跑

### 账目
- 台账 86 条唯一。**表格事故**：双列拼表致李工误读"9月3日18条"（实为 09-16 清库 18 条，已给逐条核对明细）。教训：**账目表格一律单列**。
- 美东 09-18 关账 1/5（3qXd5vg0）；美东 09-19 目标 = 5 + 欠账 4 = 9。

### 项目清理提案（已提给李工，等拍板，未动手）
- src 107 脚本 = 活水 8（leg_lab/pnl_corr/mine_batch181/mine_w200/w201/w203/submit 链/git_snapshot）+ 可清 85（56 个 mine_batch120~178、6 个旧提交件含 blind_submit/corr 探针、15 个一次性 tools、archive/expr_library 487KB）。
- 方法论打架根源 = 结论更新后旧文档没人回头改。方案：①《方法论总纲》00_总纲.md 唯一事实源（每条带日期，被推翻的标"已推翻+日期"）②旧 6 份 methodology 降级历史附录 ③删冗余脚本 ④CLAUDE.md 瘦身。
- **红线：docs/exam、docs/study（面试/备考材料）不动。**

### w203 低共享扩展批（美东 09-19 主攻）
- 生成器 `src/mine/mine_w203_expand.py`：w200 配方铺全权重网格，MIN_S=2.0 / MAX_SHARED=0.12 / MAX_PRED=0.65 / TAKE=20。
- 20 条产出（S 2.70~2.90，pred 0.61~0.65，shared 0.09~0.12），撞点多为目标自家日内兄弟 3qXd5vg0（已入池，pred 已含）。

## 12:20~13:30 w203~w208 五批连打：1 条过墙 + 三条新标定

### 战果
- **w203_09（zq8Xeo2R）过墙提交**：本地 corr 0.6832 → 平台 selfCorr 0.6906 直通，SF 4.27/tS 2.18，美东 09-19T00:32。台账 87，Super Alpha 差 13。
- w204 跨几何批（16 腿单腿建库 + 16 组合）：3 条质量达标但 0 过墙（corr 0.71~0.75）。
- w205 严格 pred≤0.58 批：16 条模拟 SF 1.4~2.1 全灭（质量崩）。
- w206 精调批：9 条模拟 5 条强达标（SF 5.0~5.2/tS 3.4+）但 0 过墙——全撞 3qXd5vg0，corr 0.79~0.84。
- w207 评级轴探针 6 条全灭（SF −0.09~0.92）：**旧判死"vec_avg 评级轴 SF≤0.88"实测确认**（R_raw_b 复现 0.88），Research16"偏离度轴可用"在本账号不成立，方法论矛盾销案。
- w208 隔夜/vwap 主腿批：21000 组合评估，约束下 **0 条** S≥2.0——矿脉不存在。

### 三条新标定（本轮最大产出，比提交本身值钱）
1. **主腿占比定律**：主腿权重/总权重 >0.5 时偏移失控（w206：pred 0.59 → real 0.81，+0.22）；≤0.4 时可信（w200_05 占比 0.4，偏移 +0.04）。低权装饰腿的"去相关贡献"是幻觉——group_rank 截面结构会压制低权腿。
2. **跨几何锚偏移 +0.08~+0.10**（w204），比 subindustry 锚（+0.05）大一倍。
3. **pred 与质量存在交叠区**：日内轴可信区 pred 0.61~0.65 且主腿占比 ≤0.45，两条日内兄弟（P_int20=3qXd5vg0、P_int10=zq8Xeo2R）已占住该区，参数空间内无法绕开。

### 账
- 美东 09-19 = 1/9；欠账 8；台账 87；Super Alpha 差 13。今日模拟 77 条、判决 28 条、提交 1 条。
- 日内/隔夜/vwap/跨几何/评级轴全部实测关闭。新生成器沉淀：mine_w203_expand / w204_crossgeom / w205_hybrid / w206_finetune / w208_overnight。

## 13:30~14:00 方法论总纲落地 + 代码清理 + CLAUDE.md 瘦身（李工批准执行）

### 交付物
- **`docs/methodology/00_总纲.md`** 建立 = 唯一事实源：有效打法（§1）、判死总表 19 条带日期（§2，含 3 条"已推翻"明示）、参数标定/偏移三定律（§3）、三条铁律（§4）、判决机制（§5）、**踩坑记录 15 条**（§6，每条根因+对策，回答李工"踩坑有没有记下来"）、研报移植方法论（§7）。
- 旧 01~06 文档共 7 份头部加"[历史附录·2026-09-19 降级]"声明，内容不动（证据链）；README.md 重写为总纲入口 + 维护规则（新结论只写总纲、附录只读）。
- **代码清理 79 个**：mine_batch120~180 共 58 个（留 181）、submit 旧件 6 个（blind_submit/corr_only/corr_probe_sweep/corr_sweep_once/precheck_only/probe_corr_service）、一次性 tools 15 个。保留：fetch_alphas/fetch_learn_*/merge_video_notes、submit_v2/run_queue_v3/verdict_dump、archive/expr_library。**台账/json/判死表未动。**
- **CLAUDE.md 瘦身**（364→约 150 行）：只留红线、执行纪律、流水线口径、目录/命令、平台约束速查；方法论细节全部指向 00_总纲。术语表随原文件留 git 历史（备考对照表在 docs/exam）。

### git
- 回滚点 **37317a4**（删前，含总纲）；清理提交 **3d62626**（79 删 + CLAUDE.md）。
- ⚠️ **推送未成功**：本地代理隧道反复 502/SSL EOF/Empty reply（HTTP/1.1 降级也失败，已重试 8 次）。非凭据问题（401 后 GCM 正常，Windows 凭据库 github.com 条目在）。**代理恢复后跑 `git_snapshot.py --push-only --allow-public` 即可**，本地领先远端 11 个提交。

### 异常记录（如实）
- 清理执行时发现 49 个 batch 文件（120~170）在脚本运行前已不在磁盘（疑似执行器重试叠加，第一批删除后中途被掐、第二次重跑报 missing）。**最终态与批准清单逐项核对一致（git status D 清单 = 79 项）**，HEAD 树完整可回滚，无多删无漏删。

## 17:20~17:30 李工令：两条硬规则入 CLAUDE.md + 欠账实测

- **CLAUDE.md 新增两节（0919 李工定）**：①「每日目标的硬定义」——5 个 = 确定提交，计数只认台账（ACTIVE + dateSubmitted 美东 + 台账追加三证齐全），模拟达标/POST 201/以为提交了都不算，每日收尾必跑台账复核；②「卡盘停机检查规则」——同目标 2~3 小时 0 提交必停查因；连报"平台故障"= 危险信号先默认自己错（大平台不可能连几天故障不解决，0914~0915 假 PENDING 即此）；报故障过三关（工具自证/平台佐证/超 4 小时按自己问题查）；卡住三查：工具→标定→假设。
- **总纲 §6 踩坑表补 #16**：卡盘不停 + 连报故障（0914~0919）。
- **欠账实测（ledger_audit 脚本，禁止凭印象）**：台账 87 行 / 86 唯一 id；美东每日：09-13=3、09-14=4、09-15=1、09-16=18（清库日）、09-17=5、09-18=1、09-19=1。**两种口径，已如实报李工请定调，未擅自定**：①现行自动化口径（09-16 清库日视为历史欠账清零，09-17 目标恢复 5）→ 欠账 = 09-18 缺 4 + 09-19 尚缺 4 = **8**（今日目标 9 已 1）；②纯累计不抵扣（从 09-13 起）= **15**（09-16 超额 13 条是否抵历史欠 7，待李工一句话）。

## 16:00~16:40 定时自动轮 —— w209 八几何探针判死 + 台账重复行发现 + 远端补推完成

### 账目核对（实测）
- 台账 87 行 / **唯一 ID 86**：`YP57jVzo`（0909 批次，group_rank(fnd6_mfma2_revt/assets,...) 族）记了 2 行，其中一行缺 dateSubmitted。红线只追加，未动，**待李工定夺是否补一行更正记录**。汇报口径暂以唯一 ID 86 计。
- 美东 09-19 = 1/9（zq8Xeo2R）；欠账 8；Super Alpha 差 13。
- 未提交达标候选全量盘点 = **702 条**，全部已被判决覆盖：456 积压 0916 全筛（最低 corr 0.6934 > 0.685，且池子只增不删、旧成员 corr 静态，该下限只升不降）；0916 后新增者来自 w196~w208 各批，逐批 0 过墙。重筛无意义。
- **PowerShell 工具 stdout 捕获本轮为空（两次复验）**，按踩坑 #1 走"写文件再读"，实际用 Bash 工具 + 落文件绕过（本轮 Bash 可用）。

### w209 经典风格几何主腿探针（8 腿单腿建库，batch181 执行器）
- 探针：低波 60/120、长反转 250/120、波动率收缩、量能异动、流动性变化、中期动量 120（全 subindustry 包裹，delay1/decay10）。
- **结果 0/8 可用**：standalone S = lowvol60 0.03 / lowvol120 0.00 / ltrev250 0.30 / ltrev120 0.26 / volcontr -0.13 / vsurp -0.11 / liqchg 0.64（tS -1.8 反向）/ mom120 -0.01。对照过墙主腿门槛（P_int20 S 2.02）差一个数量级 → **经典风格几何撑不起低共享组合，判死（总纲 §2 #20）**。
- 本轮提交 0。未写新生成器（探针复用 leg_combos json + batch181，落 `_autologs/leg_combos_w209.json`）。

### git
- **远端补推完成**：代理恢复，`git_snapshot.py --allow-public` 一次推送成功，`origin/main..main` 差 = 0，昨日积压的 11 个提交已全部上去。

## 22:04~22:30 YP57jVzo 重复记账核实 + 台账计数口径更正（李工问询后闭环）

### 判定：重复记账，不是重复提交（平台为事实源）
- GET /alphas/YP57jVzo = **ACTIVE / OS / GOOD，仅提交过一次**（2026-09-09T03:27:30-04:00），S2.14/F1.91 与两行一致。
- 两行差异：行1有 selfCorr=0.6804 缺 dateSubmitted；行2有 dateSubmitted 缺 selfCorr——0909 批提交器记录痕迹（先记判决再补落地，第二次落库时没带 selfCorr）。
- 处置（红线只追加）：追加 1 行更正行（batch 字段 = "2026-09-19-correction:..."，把两行缺失字段合并补全并写明判定依据）。台账 88 行 / 唯一 ID 86。
- **计数口径更正：有效提交 = 86（此前"台账 87 / Super Alpha 差 13"被重复行多算 1）→ Super Alpha 差 = 14。** 以后台账总数一律按唯一 id 计。

### 状态回答（李工问"又停止了？"）
- 16:00 自动轮正常跑完（w209 八几何探针 0/8 判死），不是卡死。0 提交根因 = 本地可及轴全部实测关闭（判死表 20 条）+ 702 达标候选全被 corr 墙挡住。解锁只差：平台新权限（李工侧）或新研报/新思路（移植流水线现成）。
- git：更正已本地提交（c1a529a + 1e70a4e），推送遇代理 502/SSL 断流重试 3 次未过 → 本地已完成、远端待补推（代理恢复后 --push-only）。
- 工具：Bash PATH 再次损坏（cat/dirname 127）；PowerShell stdout 捕获空 + *> 重定向 UTF-16——全程 Write 落脚本 + python 写 out 文件 + Read 读结果绕过。

## 22:33~24:50 社区灵感三帖移植（w210/w211/x212）：几何验证池外但信号弱，最近 miss corr 0.6968

### 三帖转录
- `docs/research/BRAIN_社区灵感三帖_20260919.md`。帖 1 全文可读（ts_arg_min 事件时点 + arc_tan）；帖 2 部分可辨（热度 ts_zscore(ts_sum(volume,20),252) × (-ts_corr(...)) 交互族）；帖 3 原图 430px 宽，字段/表达式不可辨，已请李工给原帖链接。

### 实测轨迹（17 腿 → 5760/9600 组合离线 → 40 组合模拟 → 8 中性化转换）
- **argmin 事件时点族 = 真·池外几何**：单腿 vs 86 条池子 maxcorr：argmin20 **0.039**、argmaxvol20 0.086、argmin60 0.167、argmin60s 0.210（对照：heat_c_pv20 0.653）。李工"池子没满"的判断在几何层面成立。
- 但信号强度不够：17 腿最强 argshort10 standalone S 1.36，过墙主腿门槛 2.02（P_int20）。热度交互族 S 0.4~0.6。
- 40 组合模拟：4 条唯一质量达标（SF 4.04~4.54），本地 corr 0.6968~0.7359，全被 0.685 直通线挡住。**最近 miss：YPbnYzol 0.6968，差 0.012**。
- 偏移标定第 N 次应验：pred 0.60~0.62 → real 0.70~0.74，shared 0.105 → 偏移 +0.09~0.12（标定区间 +0.07~0.11 内）。
- 钳形困境：eng 0.5 → corr 超线；eng ≤0.25 → tS 全塌（0.96~-0.05）；SECTOR/MARKET 转换 8 条全质量崩（SF 3.24~3.89，argmin 信号一半活在 subindustry 结构里）。
- hump(x, 0.01) 本平台报"只收 1 参数"（记录，防再踩）。

### 结论与下一步
- 本轮提交 0。**argmin 轴未判死**（corr 面极干净），死的是"裸 argmin 信号强度"。下轮方向（按帖 1 原意）：**事件条件化**——if_else 回撤超 10% 再取 ts_arg_min（`if_else(ts_min(returns,60) < -0.10, ts_arg_min(returns,60), 0)` 型），把弱信号聚焦到事件子集；或 argmin 腿以 0.5~0.75 权嫁接进 w200 已验证低共享骨架。
- 账目不变：本轮 0 提交，台账 88 行/唯一 86，美东 09-19 = 1/9，欠账 8，Super Alpha 差 14。
