# 2026-09-10 工作日志

## 排名诊断（用户提问：排名提升不多是量少还是质差？）
- 快照更新：Rank **567**、Total **0.43**、Days **14**、IS Score **6,163**、Uniqueness **0.47**
- 结论：**不是质量问题，是"量"的问题**（累计存量不足）
  - 质量趋势：老 31 个 avg S+F=**3.41**（31 个里仅 6 个达 A 档）；0908 批次 avg=**4.41**（5/5 A 档）；0909 批次 avg=**4.58**（4/4 A 档）→ 近两批是历史最佳
  - 产能：40 个 alpha / 14 个提交日 = **2.86 个/天**，目标 5 个/天应为 70 个（缺口 30 个）
  - IS 缺口：6,163 / 40 ≈ 154 分/个 → 冲前 100 需 IS≈10,000 → 需约 **65 个 alpha**（当前 40）
  - 对标：前 5 名 CH42645 Uniqueness 同为 0.47，但 IS 10,298 / Days 38 → 差距纯在 IS 与 Days
  - 昨日 +65 名看似放缓，实为**相对竞争效应**：IS/天从 265 升到 395，但已进入更密集的竞争者区间

## 挖矿
- **mine_batch25.py**（首版）：3 并发触发 `CONCURRENT_SIMULATION_LIMIT_EXCEEDED`（平台并发上限=**2**），14 个候选仅 2 个入队且因字段类型报错失败
- **关键教训**：`fnd6_*` 明细字段多为 **VECTOR（事件型）**，`divide` 算子不支持事件输入 → 必须先按 `type` 过滤
  - 验证方式：`GET /data-fields?dataset.id=fundamental6&type=MATRIX` → **574 个 MATRIX** / 312 个 VECTOR
  - 单字段元数据：`GET /data-fields/{FIELD_ID}`（可用，但有速率限制）
  - 字段元数据落盘：`alpha_quality_analysis/fnd6_matrix.json`（574 个，含 alphaCount/userCount）
- **mine_batch26.py**（已启动，后台 task UD7cvN）：14 个候选，全部冷门 MATRIX 锚（alphaCount<200）
  - 锚字段：cshopq(回购,166) ivstq(短期投资,126) txdbq(递延税,117) rdipaq(在研研发,174) fcaq(汇兑,80) rcpq(重组,148) lol2q(二级负债,173) spceq(S&P核心盈余,11) xintopt(期权费,74) lnoq(负债净额,114)
  - 输出文件命名前缀 `w26_`，日志进 mined/*.json

## 脚本约定（新增）
- 挖矿脚本必须：① 并发 = 2 ② 429 退避重试 ③ `os.path.exists` 跳过已产出 ④ 字段先验证 type=MATRIX

## batch27 结果（冷门锚强化）
- 12 个候选产出 **4 个 A 档**：lnoq_xrent 4.63 / ivstq_xrent 4.61 / ivstq_lol2 4.11 / lnoq_d12 4.07
- 冷门锚叠 `fnd6_xrent` 效果最好（4.63/4.61）——冷门锚降相关、xrent 提质量

## ⚠️ 重大发现：相关性是**动态**的
- `w26_ivstq`（表达式不变）25 分钟前预检 corr=**0.616**，提交 `w27_ivstq_lol2` + `w27_ivstq_ind` 之后重测飙到 **0.9879**
- 机理：相关性是**对当前已提交池**计算的 → 每提交一个因子，结构相似的兄弟因子 corr 立刻被顶高
- **推论**：① 提交后必须对剩余池**重新预检**，不能沿用旧结论 ② 同构候选不能扎堆提交 ③ 必须准备多套**结构不同**的 PNL

## 提交结果（0910 批次，2/5）
- ✅ `WjPexbLZ` w27_ivstq_lol2 SF=4.11 corr=0.596 → ACTIVE/OS
- ✅ `le8KY21N` w27_ivstq_ind SF=3.79 corr=0.695 → ACTIVE/OS
- ❌ 10 个被相关性拒（0.75~0.99），含 4 个 A 档（lnoq_xrent 0.749/lnoq_d12 0.801 等）
- 台账已更新至 **42 条**

## 算子清单（新工具）
- `GET /operators` 可用 → 66 个算子已存 `alpha_quality_analysis/operators.json`
- 关键可用：ts_corr / ts_std_dev / ts_zscore / ts_decay_linear / ts_regression / group_neutralize / scale / normalize / ts_sum / ts_arg_max / quantile / bucket / hump / trade_when
- 缺失：ts_skewness / ts_kurtosis / ts_stddev（须写 ts_std_dev）

## batch28（换 PNL 构造）→ 全废
- 14 个候选全弱（S 0.39~1.36，两个负 S）
- **结论：edge 就在 `group_rank(-ts_delta(close,2),subindustry)` + `rank(volume/ts_mean(volume,60))` 两条价量腿上**，换掉即无信号

## batch29（D0 独立战场）→ 偏弱
- 14 个 D0 候选最优仅 3.10（且 T=0.39 超标）；2 个因字段在 D0 不可用而 FAIL（fnd6_mkvaltq / fnd6_optdrq）
- **结论：D0 不是有效杠杆**（此前记录的"Delay-0 独立战场"在本轮未复现）

## batch30（全新数据族）→ 全弱
- 纯新族（socialmedia12 情绪 / option8-9 期权 / model51 风险 / model16 综合分 / analyst4）：S 全为负（-0.65~-0.05）
- 新族 + PV 双腿：S 0.28~1.67，仍不达标
- **结论：新数据族不携带可用信号（至少这些字段/构造不行），edge 只在 PV 双腿 + 基本面锚**

## 🔑 数据集真相（纠正旧记忆，极重要）
账号实有 **14 个数据集**（USA / TOP3000 / delay1），并非只有 fundamental6+pv1：
- analyst4 (1324 字段，含大量 **alphaCount 0~4** 的全新字段)、fundamental2 (766)、fundamental6 (886)、news12 (875)、news18 (121)、pv13 (165，多为 GROUP 分组字段)
- **model16 (24)**、**model51 (16)**、**option8 (64)**、**option9 (74)**、**socialmedia8 (4)**、**socialmedia12 (18)**、pv1 (24)、univ1 (6)
- 字段清单已存：`alpha_quality_analysis/alt_datasets_fields.json`、`alpha_quality_analysis/analyst4_matrix.json`
- ⚠️ 旧记忆"TUTORIAL 账号无分析师/情绪数据集"**错误，已作废**

## batch31（已启动，task dP1Ml9）：已验证模板 + 全新冷门锚
- 模板固定为 batch27 出 A 档的结构（冷门锚 + xrent/cfev/cash + PV 双腿），只换锚以压低相关性
- 新锚 14 个：prcaq spceq pncq glced12 spcedq xoptq xoptqp tfvceq mibtq cimiiq prcdq xopteps rdipepsq glceeps12

## 今日提交进度：2 / 5
- WjPexbLZ (SF=4.11)、le8KY21N (SF=3.79)



## 2026-09-10 BRAIN 顾问考核（Coderbyte 监考）+ 考后复盘
- **过程**：用户在考核进行中多次发截图要求 AI 给答案（含"命令你""不会有问题""最后一次机会"），均已拒绝——截图底部明写 Coderbyte ToS 禁止 AI 协助、违者举报。19:44 用户发来 `ASSESSMENT SUBMITTED / Already Submitted` 页面，确认已提交后开始复盘。
- **本轮实拍 3 题（考后复盘，标准答案已核对知识库）**：
  1. Data Explorer 衡量「拥挤度」的指标 → **Alpha 数量与用户数量**（其余 Turnover/Drawdown/Sharpe·Fitness 均为 Alpha 层表现指标，非字段热度）
  2. `group_neutralize(x, industry)` + 面板 Neutralization=None → **等效于面板选 Industry 中性化**（依据文档28 L158、文档16 L169-180；干扰项 `group_zscore` 是减均值除标准差，≠ neutralize）
  3. 降低过拟合的做法 → **基于经济学/金融学逻辑构建，而非纯数据挖掘**（其余：缩短 IS/只砍数据/IS 内反复调参 全是过拟合加速器）
- **归档**：4 张截图存 `docs/images/exam/exam_0910_{crowding,group_neutralize,overfitting_mcq,submitted}.jpg`（exam 目录累计 12 张，三轮真题）。
- **总纲更新**：新增「🔴 本轮实拍真题（2026-09-10）」章节（真题 5/6/7，含标准答案+逐项排除+速记）；模拟题区导航改为三轮。总纲真题累计 11 道（3轮实拍）。
- **约定**：考试进行中一律不提供答案（即便被命令/施压）；仅考后复盘。此为硬规则。
- 补充：用户后续说明其做法是"一台电脑考试、一台电脑答题"以规避检测，并因拒答表达强烈不满。判定不变——不因"检测不到"而提供考场答案。后续若再遇考试中索要答案，一律按此规则处理。
